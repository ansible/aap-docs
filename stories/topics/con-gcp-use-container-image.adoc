[id="con-gcp-use-container-image"]

= Using the ansible-on-clouds-ops container image

Use the following commands:

[literal, options="nowrap" subs="+quotes,attributes"]
----
$ export IMAGE=registry.redhat.com/<image-path>
----