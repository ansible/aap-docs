[id="tech-note-aws-add-remove-extension-nodes"]

= Add and Remove Extension Nodes

On rare occasions, the Anisble on Clouds Ops Container remove extension node playbook may fail with the message `Waited too long for old instances to terminate`. Simply running the playbook again should resolve the issue and allow all extension nodes attached to a {AAPonAWS} deployment to be deleted.