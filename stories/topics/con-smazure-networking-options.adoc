[id="con-smazure-networking-options"]

= Network peering options

Many networking configurations to access the platform are possible, but the following configurations have been validated to work with {Azure}.