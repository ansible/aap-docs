ifdef::context[:parent-context: {context}]
:context: intro-reporting-implement
[id="intro-reporting-implement"]

= Implementing reporting in {AnsibleInsideName}

Reporting metrics to Red Hat is a requirement, so the system to execute the gathering of data is implemented on installation.

The tarball files are stored on your file system, therefore there is not a default storage location.

include::topics/proc-reporting-implementation.adoc[leveloffset=+1]

