# Red Hat Ansible Inside

The ansible-inside-x.y branches contain the documentation for Ansible Inside releases.

## Working with the Ansible Inside documentation

The Ansible Inside docs are synced to the production repo in Gitlab.
The docs are stored in the ansible-inside directory.

