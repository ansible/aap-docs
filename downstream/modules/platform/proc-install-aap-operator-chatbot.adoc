
[id="proc-chatbot-install-aap-operator"]

= Installing and configuring the {PlatformNameShort} operator on {OCPShort}

Install and configure the Ansible Automation Platform operator on the Red Hat OpenShift Container Platform, so that you can deploy and manage the Ansible Lightspeed intelligent assistant component.

== Install the {PlatformNameShort} operator
Install the {PlatformNameShort} operator on {OCPShort}, so that you can deploy and manage the {AAPchatbot} component.

include::proc-install-aap-operator.adoc[leveloffset=+1]

* Verify that the {PlatformNameShort} operator displays a *Succeeded* status. 

include::proc-operator-link-components.adoc[leveloffset=+1]

You must also verify that all the pods are running successfully. Perform the following steps:

. Click menu:Workloads[Pods].
. Switch to the project named as described in the namespace metadata in the *YAML* configuration view.
. Verify that all pods display either a *Running* or *Completed* status, with no pods displaying an error status.
