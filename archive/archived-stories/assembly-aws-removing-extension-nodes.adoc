:_mod-docs-content-type: ASSEMBLY

ifdef::context[:parent-context: {context}]

[id="assembly-aws-remove-extension"]
= Removing extension nodes

:context: aws-extension-nodes

You must create a backup of the {PlatformNameShort} deployment before removing extension nodes.

Follow these steps to remove execution nodes from your {AAPonAWS} environment.

.Prerequisites
* Linux or macOS system (where the `ansible-on-clouds-ops` container image will run)
* Docker

.Steps
. Pull the `ansible-on-clouds-ops` container image.
. Generate data files by running the ansible-on-clouds-ops container.
. Update the data file.
. Run the `ansible-on-clouds-ops` container to remove the extension nodes.

include::topics/con-aws-pull-container-image.adoc[leveloffset=+1]
:context: remove
include::topics/ref-aws-permissions-to-manage-nodes.adoc[leveloffset=+1]
include::topics/proc-aws-generate-remove-data-files.adoc[leveloffset=+1]
include::topics/proc-aws-update-remove-data-file.adoc[leveloffset=+1]
include::topics/proc-aws-removing-extension-nodes.adoc[leveloffset=+1]
